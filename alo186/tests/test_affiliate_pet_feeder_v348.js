const assert=require("assert");const fs=require("fs");const path=require("path");const vm=require("vm");
const ROOT=path.resolve(__dirname,"..","..");const slug="pil-yedekli-otomatik-mama-makinesi-elektrik-kesintisi-secimi";const route=path.join(ROOT,"alo186","amazon-elektrik-urunleri",slug);const html=fs.readFileSync(path.join(route,"index.html"),"utf8");const catalogText=fs.readFileSync(path.join(route,"catalog-v348.js"),"utf8");const appText=fs.readFileSync(path.join(route,"app-v348.js"),"utf8");const overlay=JSON.parse(fs.readFileSync(path.join(ROOT,"alo186","deployment","routing-overlays","348-affiliate-pet-feeder.json"),"utf8"));const riskPolicy=JSON.parse(fs.readFileSync(path.join(ROOT,"alo186","deployment","affiliate_route_risk_policy_v265.json"),"utf8"));
const sandbox={module:{exports:{}},exports:{},globalThis:{}};vm.runInNewContext(catalogText,sandbox,{filename:"catalog-v348.js"});const catalog=sandbox.module.exports;
assert.strictEqual(catalog.version,348);assert.strictEqual(catalog.affiliateTag,"alo186rehber-21");assert.strictEqual(catalog.verifiedAt,"2026-08-14");assert.strictEqual(catalog.maxVerificationAgeDays,45);assert.strictEqual(catalog.category.slug,slug);assert.strictEqual(catalog.category.risk,"consumer-low");assert.strictEqual(catalog.category.affiliatePolicy,"after_tool");assert.strictEqual(catalog.category.professionalOnly,false);assert.strictEqual(catalog.category.highRiskDirectCta,false);assert.ok(riskPolicy.governedAffiliateRoutePatterns.includes("pil-yedekli-otomatik-mama-makinesi"));assert.strictEqual(catalog.products.length,3);
const expectedAsins=["B0C5DGR1PB","B0CH49H9NZ","B0DN9Z6P31"];const expectedMpns=["HST-QQ005","HST-QQ003","BHR9030EU"];assert.deepStrictEqual(Array.from(catalog.products,p=>p.asin),expectedAsins);assert.deepStrictEqual(Array.from(catalog.products,p=>p.mpn),expectedMpns);assert.strictEqual(new Set(expectedAsins).size,3);assert.strictEqual(new Set(expectedMpns).size,3);
for(const product of catalog.products){assert.match(product.asin,/^[A-Z0-9]{10}$/);assert.ok(product.brand&&product.userNeed&&product.noBuyWhen);assert.ok(product.strengths.length>=3);assert.ok(product.limitations.length>=3);assert.match(product.noBuyWhen,/satın almayın/);assert.strictEqual(product.amazonAsinSource,`https://www.amazon.com.tr/dp/${product.asin}`);assert.ok(product.amazonActivityEvidence);assert.match(product.technicalSource,/^https:\/\/(www\.haustier\.com\.tr|www\.mi\.com)\//);assert.strictEqual(catalog.verificationStatus(product,new Date("2026-09-28T00:00:00Z")).fresh,true);assert.strictEqual(catalog.verificationStatus(product,new Date("2026-09-29T00:00:00Z")).fresh,false);const url=catalog.amazonProductUrl(product,new Date("2026-08-14T12:00:00Z"));assert.ok(url.includes("/dp/"+product.asin));assert.ok(url.endsWith("?tag=alo186rehber-21"));assert.strictEqual(catalog.amazonProductUrl(product,new Date("2026-09-29T00:00:00Z")),null);}
const canonical="https://alo186.com/amazon-elektrik-urunleri/pil-yedekli-otomatik-mama-makinesi-elektrik-kesintisi-secimi/";assert.ok(html.includes(`<link rel="canonical" href="${canonical}">`));assert.ok(html.includes('data-commercial-scope="after_tool"'));assert.ok(html.includes('data-professional-only="false"'));assert.ok((html.match(/Görünür satış ortaklığı açıklaması/g)||[]).length>=3);assert.strictEqual((html.match(/rel="sponsored nofollow noopener"/g)||[]).length,3);assert.ok(!/href="https:\/\/www\.amazon\.com\.tr\/dp\/[^\"]+\?tag=/.test(html));assert.ok(!/"@type"\s*:\s*"Offer"|priceCurrency|aggregateRating|ratingValue|reviewCount|availability/.test(html));assert.ok(html.includes("duman, kıvılcım, yangın veya elektrik çarpması"));assert.ok(html.includes("Mevcut çözümüm yeterli"));assert.ok(html.includes("yeni ürün almayacağım"));
const ids=Array.from(html.matchAll(/\sid="([^"]+)"/g),m=>m[1]);assert.strictEqual(new Set(ids).size,ids.length,"duplicate HTML id");for(const anchor of ["arac-kesinti-hazirlik","karar-satin-almama","karsilastirma-pil-yedekli-otomatik-mama-makineleri","urun-haustier-qq005-hst-qq005","urun-haustier-qq003-hst-qq003","urun-xiaomi-smart-pet-food-feeder-2-bhr9030eu"])assert.ok(ids.includes(anchor));
const kgMatch=html.match(/<script id="knowledge-graph" type="application\/ld\+json">\s*([\s\S]*?)\s*<\/script>/);assert.ok(kgMatch);const graph=JSON.parse(kgMatch[1])["@graph"];assert.strictEqual(graph.filter(n=>n["@type"]==="Product").length,3);assert.strictEqual(graph.filter(n=>n["@type"]==="Brand").length,2);assert.strictEqual(graph.filter(n=>n["@type"]==="ItemList").length,1);assert.strictEqual(graph.filter(n=>n["@type"]==="BreadcrumbList").length,1);const graphIds=graph.map(n=>n["@id"]).filter(Boolean);assert.strictEqual(new Set(graphIds).size,graphIds.length,"duplicate JSON-LD @id");for(const node of graph.filter(n=>n["@type"]==="Product")){assert.ok(node.identifier.some(i=>i.propertyID==="ASIN"));assert.ok(node.identifier.some(i=>i.propertyID==="MPN"));assert.ok(node.additionalProperty.length>=4);assert.ok(!("offers" in node));}for(const asin of expectedAsins){assert.ok(html.includes(`"value":"${asin}"`));assert.ok(html.includes(`data-affiliate-asin="${asin}"`));}
assert.ok(appText.includes('catalog.category.affiliatePolicy!=="after_tool"'));assert.ok(appText.includes("catalog.category.professionalOnly"));assert.ok(appText.includes("catalog.category.highRiskDirectCta"));assert.ok(appText.includes('link.rel="sponsored nofollow noopener"'));assert.ok(appText.includes('link.removeAttribute("href")'));for(const gate of ["gateTask","gateFood","gateSchedule","gateOutageTest","gateHumanBackup","gateNoHazard","gateVariant","gateNeed","gateAffiliate"])assert.ok(html.includes(`id="${gate}"`));
function walk(dir){if(!fs.existsSync(dir))return[];return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>{const full=path.join(dir,e.name);return e.isDirectory()?walk(full):[full];});}const catalogFiles=walk(path.join(ROOT,"alo186","amazon-elektrik-urunleri")).filter(f=>/catalog-v\d+\.js$/.test(f));for(const asin of expectedAsins){let occurrences=0;for(const file of catalogFiles){const matches=fs.readFileSync(file,"utf8").match(new RegExp(`asin\\s*:\\s*["']${asin}["']`,`g`));occurrences+=matches?matches.length:0;}assert.strictEqual(occurrences,1,`global duplicate ASIN: ${asin}`);}
assert.strictEqual(overlay.version,348);assert.strictEqual(overlay.routes.length,1);assert.strictEqual(overlay.routes[0].canonicalPath,"/amazon-elektrik-urunleri/pil-yedekli-otomatik-mama-makinesi-elektrik-kesintisi-secimi/");assert.strictEqual(overlay.routes[0].source,"alo186/amazon-elektrik-urunleri/pil-yedekli-otomatik-mama-makinesi-elektrik-kesintisi-secimi/index.html");
console.log(JSON.stringify({ok:true,version:348,products:catalog.products.map(p=>({asin:p.asin,mpn:p.mpn})),checks:["canonical","affiliate-tag","sponsored-nofollow-noopener","duplicate-ASIN-global","stale-45-46","Knowledge-Graph","Product-Brand-ItemList","ASIN-MPN","additionalProperty","duplicate-id","after_tool","professional_only","high-risk-direct-cta","no-Offer","visible-disclosure","no-buy","active-hazard-close"]},null,2));
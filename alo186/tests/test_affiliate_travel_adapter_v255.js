const assert=require("assert");const fs=require("fs");const path=require("path");const vm=require("vm");
const ROOT=path.resolve(__dirname,"..","..");const slug="seyahat-priz-adaptoru-fis-voltaj-uyumluluk-secimi";const route=path.join(ROOT,"alo186","amazon-elektrik-urunleri",slug);const html=fs.readFileSync(path.join(route,"index.html"),"utf8");const catalogText=fs.readFileSync(path.join(route,"catalog-v255.js"),"utf8");const appText=fs.readFileSync(path.join(route,"app-v255.js"),"utf8");const overlay=JSON.parse(fs.readFileSync(path.join(ROOT,"alo186","deployment","routing-overlays","255-affiliate-travel-adapter.json"),"utf8"));
const sandbox={module:{exports:{}},exports:{},globalThis:{}};vm.runInNewContext(catalogText,sandbox,{filename:"catalog-v255.js"});const catalog=sandbox.module.exports;
assert.strictEqual(catalog.version,255);assert.strictEqual(catalog.affiliateTag,"alo186rehber-21");assert.strictEqual(catalog.verifiedAt,"2026-08-04");assert.strictEqual(catalog.maxVerificationAgeDays,45);assert.strictEqual(catalog.category.slug,slug);assert.strictEqual(catalog.category.risk,"consumer-medium");assert.strictEqual(catalog.category.affiliatePolicy,"after_tool");assert.strictEqual(catalog.category.professionalOnly,false);assert.strictEqual(catalog.category.highRiskDirectCta,false);assert.strictEqual(catalog.products.length,3);
const expectedAsins=["B005P21MNW","B003ZID1SC","B0C8H1ZFQS"];const expectedMpns=["1.500211E","1508550","UA11"];assert.deepStrictEqual(Array.from(catalog.products,p=>p.asin),expectedAsins);assert.deepStrictEqual(Array.from(catalog.products,p=>p.mpn),expectedMpns);assert.strictEqual(new Set(expectedAsins).size,3);assert.strictEqual(new Set(expectedMpns).size,3);
for(const product of catalog.products){assert.match(product.asin,/^[A-Z0-9]{10}$/);assert.ok(product.brand&&product.userNeed&&product.noBuyWhen);assert.ok(product.strengths.length>=3);assert.ok(product.limitations.length>=3);assert.match(product.noBuyWhen,/satın almayın/);assert.strictEqual(product.amazonAsinSource,`https://www.amazon.com.tr/dp/${product.asin}`);assert.match(product.technicalSource,/^https:\/\/(skross\.com|www\.brennenstuhl\.com|www\.momax\.net)\//);assert.strictEqual(catalog.verificationStatus(product,new Date("2026-09-18T00:00:00Z")).fresh,true);assert.strictEqual(catalog.verificationStatus(product,new Date("2026-09-19T00:00:00Z")).fresh,false);const url=catalog.amazonProductUrl(product,new Date("2026-08-04T12:00:00Z"));assert.ok(url.includes("/dp/"+product.asin));assert.ok(url.endsWith("?tag=alo186rehber-21"));assert.strictEqual(catalog.amazonProductUrl(product,new Date("2026-09-19T00:00:00Z")),null);}
const canonical="https://alo186.com/amazon-elektrik-urunleri/seyahat-priz-adaptoru-fis-voltaj-uyumluluk-secimi/";assert.ok(html.includes(`<link rel="canonical" href="${canonical}">`));assert.ok(html.includes('data-commercial-scope="after_tool"'));assert.ok(html.includes('data-professional-only="false"'));assert.ok((html.match(/Görünür satış ortaklığı açıklaması/g)||[]).length>=3);assert.strictEqual((html.match(/rel="sponsored nofollow noopener"/g)||[]).length,3);assert.ok(!/href="https:\/\/www\.amazon\.com\.tr\/dp\/[^\"]+\?tag=/.test(html));assert.ok(!/"@type"\s*:\s*"Offer"|priceCurrency|aggregateRating|ratingValue|reviewCount/.test(html));assert.ok(html.includes("gerilimi veya frekansı dönüştürmez"));assert.ok(html.includes("Mevcut çözüm güvenli ve yeterliyse satın alma yapmayın"));assert.ok(html.includes("Topraklama"));
const ids=Array.from(html.matchAll(/\sid="([^"]+)"/g),m=>m[1]);assert.strictEqual(new Set(ids).size,ids.length,"duplicate HTML id");for(const anchor of ["urun-skross-world-to-europe-1500211e","urun-brennenstuhl-travel-adapter-1508550","urun-momax-ua11-travel-charger"])assert.ok(ids.includes(anchor));
const kgMatch=html.match(/<script id="knowledge-graph" type="application\/ld\+json">\s*([\s\S]*?)\s*<\/script>/);assert.ok(kgMatch);const graph=JSON.parse(kgMatch[1])["@graph"];assert.strictEqual(graph.filter(n=>n["@type"]==="Product").length,3);assert.strictEqual(graph.filter(n=>n["@type"]==="Brand").length,3);assert.strictEqual(graph.filter(n=>n["@type"]==="ItemList").length,1);assert.strictEqual(graph.filter(n=>n["@type"]==="BreadcrumbList").length,1);const graphIds=graph.map(n=>n["@id"]).filter(Boolean);assert.strictEqual(new Set(graphIds).size,graphIds.length,"duplicate JSON-LD @id");for(const node of graph.filter(n=>n["@type"]==="Product")){assert.ok(node.identifier.some(i=>i.propertyID==="ASIN"));assert.ok(node.identifier.some(i=>i.propertyID==="MPN"));assert.ok(node.additionalProperty.length>=3);}for(const asin of expectedAsins){assert.ok(html.includes(`"value":"${asin}"`));assert.ok(html.includes(`data-affiliate-asin="${asin}"`));}
assert.ok(appText.includes('catalog.category.affiliatePolicy!=="after_tool"'));assert.ok(appText.includes("catalog.category.professionalOnly"));assert.ok(appText.includes("catalog.category.highRiskDirectCta"));assert.ok(appText.includes('link.rel="sponsored nofollow noopener"'));assert.ok(appText.includes('link.removeAttribute("href")'));for(const gate of ["gateDeviceLabel","gatePlugDirection","gateGrounding","gateSocketCondition","gateLowPower","gateNoCriticalUse","gateVariant","gateNeed","gateAffiliate"])assert.ok(html.includes(`id="${gate}"`));
function walk(dir){if(!fs.existsSync(dir))return[];return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>{const full=path.join(dir,e.name);return e.isDirectory()?walk(full):[full];});}const catalogFiles=walk(path.join(ROOT,"alo186","amazon-elektrik-urunleri")).filter(f=>/catalog-v\d+\.js$/.test(f));for(const asin of expectedAsins){let occurrences=0;for(const file of catalogFiles){const matches=fs.readFileSync(file,"utf8").match(new RegExp(`asin\\s*:\\s*["']${asin}["']`,"g"));occurrences+=matches?matches.length:0;}assert.strictEqual(occurrences,1,`global duplicate ASIN: ${asin}`);}
assert.strictEqual(overlay.version,255);assert.strictEqual(overlay.routes.length,1);assert.strictEqual(overlay.routes[0].canonicalPath,"/amazon-elektrik-urunleri/seyahat-priz-adaptoru-fis-voltaj-uyumluluk-secimi/");assert.strictEqual(overlay.routes[0].source,"alo186/amazon-elektrik-urunleri/seyahat-priz-adaptoru-fis-voltaj-uyumluluk-secimi/index.html");console.log(JSON.stringify({ok:true,version:255,products:catalog.products.map(p=>({asin:p.asin,mpn:p.mpn})),checks:["canonical","affiliate-tag","sponsored-nofollow-noopener","duplicate-ASIN-global","stale-45-46","Knowledge-Graph","duplicate-id","after_tool","professional_only","high-risk-direct-cta","no-Offer"]},null,2));
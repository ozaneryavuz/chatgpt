const fs=require('fs');const path=require('path');const html=fs.readFileSync(path.join(__dirname,'index.html'),'utf8');for(const token of ['id="checker"','id="result"','12 saati','on iş günü','FAQPage','WebApplication','/edas-bul']){if(!html.includes(token))throw new Error(`Eksik sözleşme: ${token}`);}if(!/addEventListener\('submit'/.test(html))throw new Error('Karar motoru submit olayı eksik');console.log('outage compensation checker: PASS');
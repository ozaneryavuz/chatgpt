FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt \
    && addgroup --system alo186 \
    && adduser --system --ingroup alo186 --home /app alo186

COPY --chown=alo186:alo186 app ./app
USER alo186

EXPOSE 8000
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/live', timeout=3)" || exit 1

CMD ["uvicorn", "app.production:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1", "--no-server-header"]

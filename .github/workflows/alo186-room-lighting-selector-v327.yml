name: ALO186 Oda aydınlatma lümen ve Kelvin seçimi

on:
  pull_request:
    paths:
      - 'alo186/hesaplama/oda-aydinlatma-lumen-kelvin-uygunluk/**'
      - 'alo186/hesaplama/index.html'
      - 'alo186/deployment/routing-overlays/090-room-lighting-selector.json'
      - '.github/workflows/alo186-room-lighting-selector-v327.yml'
  push:
    branches: [main]
    paths:
      - 'alo186/hesaplama/oda-aydinlatma-lumen-kelvin-uygunluk/**'
      - 'alo186/hesaplama/index.html'
      - 'alo186/deployment/routing-overlays/090-room-lighting-selector.json'
      - '.github/workflows/alo186-room-lighting-selector-v327.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: alo186-room-lighting-selector-v327-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Aydınlatma hesabı, güven, satın almama ve dönüşüm senaryoları
        run: |
          set -euo pipefail
          node alo186/hesaplama/oda-aydinlatma-lumen-kelvin-uygunluk/app.test.js
          node --check alo186/hesaplama/oda-aydinlatma-lumen-kelvin-uygunluk/app.js

      - name: Mevcut yayın ve güven regresyonları
        run: |
          set -euo pipefail
          node alo186/tests/test_release_manifest.js
          node alo186/tests/test_portal_hardening.js
          python alo186/tests/device_damage_deadline_guard.py

      - name: Canonical üretim ve statik site smoke testi
        run: |
          set -euo pipefail
          python alo186/deployment/build_static_site.py \
            --output /tmp/alo186-room-lighting-canonical \
            --commit "$GITHUB_SHA"
          python alo186/deployment/smoke_static_site.py \
            --bundle /tmp/alo186-room-lighting-canonical

      - name: Custom-domain ve project-path Pages üretimi
        run: |
          set -euo pipefail
          for target in custom project; do
            cp -a /tmp/alo186-room-lighting-canonical "/tmp/alo186-room-lighting-$target"
          done

          python alo186/deployment/prepare_github_pages.py \
            --site /tmp/alo186-room-lighting-custom \
            --base-path '' \
            --repository "$GITHUB_REPOSITORY" \
            --commit "$GITHUB_SHA"
          python alo186/deployment/inject_outcome_runtime.py \
            --site /tmp/alo186-room-lighting-custom \
            --base-path ''
          python alo186/deployment/inject_shortlist_growth.py \
            --site /tmp/alo186-room-lighting-custom \
            --base-path '' > /tmp/alo186-room-lighting-custom.json
          python alo186/deployment/smoke_github_pages.py \
            --site /tmp/alo186-room-lighting-custom \
            --base-path ''

          python alo186/deployment/prepare_github_pages.py \
            --site /tmp/alo186-room-lighting-project \
            --base-path '/chatgpt' \
            --repository "$GITHUB_REPOSITORY" \
            --commit "$GITHUB_SHA"
          python alo186/deployment/inject_outcome_runtime.py \
            --site /tmp/alo186-room-lighting-project \
            --base-path '/chatgpt'
          python alo186/deployment/inject_shortlist_growth.py \
            --site /tmp/alo186-room-lighting-project \
            --base-path '/chatgpt' > /tmp/alo186-room-lighting-project.json
          python alo186/deployment/smoke_github_pages.py \
            --site /tmp/alo186-room-lighting-project \
            --base-path '/chatgpt'

      - name: Rota, sitemap, teknik arama, affiliate ve gizlilik kabulü
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import re
          from pathlib import Path

          route='hesaplama/oda-aydinlatma-lumen-kelvin-uygunluk'
          slug='oda-aydinlatma-lumen-kelvin-uygunluk/'
          canonical='/hesaplama/oda-aydinlatma-lumen-kelvin-uygunluk/'

          for root in [
              Path('/tmp/alo186-room-lighting-custom'),
              Path('/tmp/alo186-room-lighting-project'),
          ]:
              page=root/route/'index.html'
              app=root/route/'app.js'
              css=root/route/'styles.css'
              assert all(path.is_file() for path in [page, app, css])

              html=page.read_text(encoding='utf-8')
              js=app.read_text(encoding='utf-8')
              assert slug in html
              assert all(token in html for token in [
                  'actualNeed',
                  'technicalCheck',
                  'affiliateCheck',
                  'Amazon satış ortaklığı bağlantısı',
                  'rel="sponsored nofollow noopener"',
                  'JSON teknik fişi indir',
                  '180 günlük yeniden kontrol',
                  'ALO186 EDAŞ, kamu kurumu',
              ])
              assert all(token not in js for token in ['localStorage', 'sessionStorage', 'fetch('])
              assert 'alo186rehber-21' in js
              assert 'commerceAllowed' in js and "state === 'no_buy'" in js
              assert 'electricalHazard' in js and "state = 'hazard'" in js
              assert 'fixedInstallation' in js and "state = 'professional'" in js
              assert '"@type":"Product"' not in html
              assert '"@type":"Offer"' not in html
              assert all(token not in html for token in [
                  '"availability":',
                  '"aggregateRating":',
                  '"review":',
                  '"warranty":',
              ])

              hub=(root/'hesaplama/index.html').read_text(encoding='utf-8')
              assert slug in hub
              assert hub.count('class="tool-card"') >= 43
              assert re.search(r'\d+ çekirdek araç',hub)

              sitemap=(root/'sitemap.xml').read_text(encoding='utf-8')
              assert slug in sitemap
              search=json.loads((root/'arama/search-index.json').read_text(encoding='utf-8'))
              entry=next(item for item in search['entries'] if item['canonicalPath'] == canonical)
              assert entry['url'].endswith(canonical)

          print(json.dumps({
              'ok': True,
              'route': canonical,
              'toolCountMinimum': 43,
              'states': ['hazard', 'professional', 'evidence', 'no_buy', 'commerce'],
              'affiliateTripleGate': True,
              'affiliateDisclosure': True,
              'unverifiedCommercialFields': False,
              'personalData': False,
              'storage': False,
              'revisitDays': 180,
              'searchIndexed': True,
          }, ensure_ascii=False))
          PY

      - name: Doğrulanmış site artifactı
        uses: actions/upload-artifact@v4
        with:
          name: alo186-room-lighting-selector-v327
          path: /tmp/alo186-room-lighting-custom
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 30

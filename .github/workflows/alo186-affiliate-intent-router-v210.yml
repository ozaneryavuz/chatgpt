name: ALO186 affiliate intent router v210

on:
  pull_request:
    paths:
      - 'alo186/deployment/inject_affiliate_intent_router_v210.py'
      - 'alo186/tests/test_affiliate_intent_router_v210.py'
      - '.github/workflows/alo186-affiliate-intent-router-v210.yml'
      - '.github/workflows/alo186-github-pages.yml'
  push:
    branches: [main]
    paths:
      - 'alo186/deployment/inject_affiliate_intent_router_v210.py'
      - 'alo186/tests/test_affiliate_intent_router_v210.py'
      - '.github/workflows/alo186-affiliate-intent-router-v210.yml'
      - '.github/workflows/alo186-github-pages.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: alo186-affiliate-intent-v210-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          package-manager-cache: false
      - name: Python ve fail-closed sözleşmesi
        run: |
          set -euo pipefail
          python -m py_compile \
            alo186/deployment/inject_affiliate_intent_router_v210.py \
            alo186/tests/test_affiliate_intent_router_v210.py
          python alo186/tests/test_affiliate_intent_router_v210.py
      - name: Canonical yayıncı entegrasyonu
        run: |
          set -euo pipefail
          grep -q 'inject_affiliate_intent_router_v210.py' .github/workflows/alo186-github-pages.yml
          ! grep -q 'actions/deploy-pages' .github/workflows/alo186-affiliate-intent-router-v210.yml
          ! grep -q 'actions/upload-pages-artifact' .github/workflows/alo186-affiliate-intent-router-v210.yml
